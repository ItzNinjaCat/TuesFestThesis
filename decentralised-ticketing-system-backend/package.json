{
  "name": "decentralised-ticketing-system",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ decentralised-ticketing-system",
    "create-local": "graph create --node http://localhost:8020/ decentralised-ticketing-system",
    "remove-local": "graph remove --node http://localhost:8020/ decentralised-ticketing-system",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 decentralised-ticketing-system",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1",
    "ipfs-core": "^0.17.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
